python eval.py

--dataset_path <-- The path in which the folder with the annotations
lies. Default ist ~/dataset/inoutdoorpeoplergbd 

--boundingboxes=<DETECTIONS>  <-- Path to file with all detections

--test_file_sequence=<TESTFILESEQ> <-- The testfile sequence in the annotations folder

Thresholds

--softmax_threshold=0.5

--iou_threshold=0.6
